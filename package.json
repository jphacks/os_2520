{
  "name": "os_2520-heroku",
  "version": "1.0.0",
  "description": "Full-stack application for Heroku deployment",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "prebuild": "node scripts/set-frontend-env.js",
    "build": "cd front && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "heroku-postbuild": "npm run build",
    "build:local": "cd front && npm install && npm run build && cd ../backend && npm install && npm run build"
  },
  "dependencies": {},
  "devDependencies": {},
  "license": "MIT"
}